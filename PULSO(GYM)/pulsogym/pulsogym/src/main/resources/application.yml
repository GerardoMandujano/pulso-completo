server:
  port: 8081

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/pulso_db
    username: root
    password: gmr110293
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect


jwt:
  secret: miSuperClaveSecreta
  issuer: miApp

logging:
  file:
   name: C:/logs/pulso_gym/pulso_gym.log       # Ruta y nombre del archivo de log
  level:
   root: INFO               # Nivel de log global
   mx.com.gmr.app.security: DEBUG    # Nivel para tu código/librería
  pattern:
   console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
   file: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"